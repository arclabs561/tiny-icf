# tiny-icf

Compressed character-level model for estimating word commonality (ICF).

Licensed under MIT.

```bash
uv sync

# Train
uv run tiny-icf-train --help

# Predict
uv run tiny-icf-predict --help
```

More: `docs/`.
